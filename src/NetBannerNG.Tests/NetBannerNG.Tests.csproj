<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net481</TargetFramework>
        <IsPackable>false</IsPackable>

        <!-- Optional but valid if your code already uses nullable annotations -->
        <Nullable>enable</Nullable>

        <!-- Avoid implicit usings for .NET Framework test projects unless you need them -->
        <ImplicitUsings>false</ImplicitUsings>

        <!-- Optional: use only if your compiler/VS version supports it -->
        <LangVersion>latest</LangVersion>
    </PropertyGroup>

    <!-- Add this only if linked files use WPF types such as System.Windows.Media.Color -->
    <!--
  <PropertyGroup>
    <UseWPF>true</UseWPF>
  </PropertyGroup>
  -->

    <ItemGroup>
        <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.11.1" />
        <PackageReference Include="MSTest.TestAdapter" Version="3.6.2" />
        <PackageReference Include="MSTest.TestFramework" Version="3.6.2" />
    </ItemGroup>

    <ItemGroup>
      <ProjectReference Include="..\NetBannerNG.Common\NetBannerNG.Common.csproj" />
      <ProjectReference Include="..\NetBannerNG\NetBannerNG.csproj" />
    </ItemGroup>

</Project>